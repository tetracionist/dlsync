---version: 0, author: DlSync
CREATE TABLE ${EXAMPLE_DB}.${MAIN_SCHEMA}.DISCOUNT_RATE_EXCEEDED_HISTORY (
    RATE_EXCEEDED_TS TIMESTAMP
);
---rollback: DROP TABLE IF EXISTS ${EXAMPLE_DB}.${MAIN_SCHEMA}.DISCOUNT_RATE_EXCEEDED_HISTORY;
---verify: SELECT * FROM ${EXAMPLE_DB}.${MAIN_SCHEMA}.DISCOUNT_RATE_EXCEEDED_HISTORY LIMIT 1;
